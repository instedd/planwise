(ns planwise.client.projects2.core)

(def sections
  [{:step "goal" :title "Goal"}
   {:step "consumers" :title "Consumers"}
   {:step "providers" :title "Providers"}
   {:step "coverage" :title "Coverage"}
   {:step "actions" :title "Actions"}
   {:step "review" :title "Review"}])
